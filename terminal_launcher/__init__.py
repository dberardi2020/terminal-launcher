"""Terminal Launcher — compose and launch tiled Claude Code sessions.

A shared core (config + composition model) behind a terminal-backend seam:
iTerm2 on macOS (native windows via its Python API). Other platforms have no
native backend yet — a Windows Terminal backend is planned.
"""

__version__ = "1.2.0"
