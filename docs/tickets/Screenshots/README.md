# Screenshots — staging inbox

Fresh captures (e.g. from `/db-screenshot`) land here. This folder is **gitignored**
except for this README — nothing here is tracked or permanent.

Triage each shot, then either:

- **Promote** it — move it into the ticket's own `../TLA-NNNN/Screenshots/` and commit
  it there as evidence, or
- **Discard** it.

Reference the *promoted* copy from a ticket, never a path in this inbox.
